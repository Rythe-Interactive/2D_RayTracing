using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RayTracerHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //RayTracer.Start();
    }

    // Update is called once per frame
    void Update()
    {
        //RayTracer.render();
    }
}
